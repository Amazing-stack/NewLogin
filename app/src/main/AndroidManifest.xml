<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newlogin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyprofileActivity"
            android:label="@string/title_activity_myprofile"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity android:name=".Splashscreen" />
        <activity
            android:name=".Stationdetails"
            android:label="@string/title_activity_stationdetails"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Offence_formActivity"
            android:label="@string/title_activity_offence_form"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Complainant_form"
            android:label="@string/title_activity_complainant_form"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".DashboardActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".RegisterActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>